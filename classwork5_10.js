// Write a function to find longest substring in a given a string without repeating characters except
// space character. If there are several, return the last one. Consider that all letters are lowercase.

// Input                                                                                    Output

// “there are no two words in the english language more harmful than "good job".”           “rmful than “go”

// “parting your soup is not a miracle, bruce.”                                             “up is not a m”



let input = "parting your soup is not a miracle, bruce.";

let output = "";
let count = true;
let start = input[0];

// for ( let i = 1; i < input.length; i++) {
//     output = input.slice(i);
//     for ()

    // if ( input[i] !== input[i-1] ) {
    //     // console.log (input[i]);
    //     output += input[i],input[i-1];
    //     // console.log(output);
    // } else continue;
// }
// console.log(output);





// ---------- NOT DONE ---------- \\